# Leave empty for local dev — Vite proxies /api → http://localhost:5000 (same DB as server/.env side5_db)
VITE_API_URL=

# Phone testing on Wi‑Fi: set your PC's LAN IP (ipconfig), ensure API is running, then restart Vite:
# VITE_API_URL=http://192.168.1.45:5000
